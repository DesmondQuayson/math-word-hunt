# Phase 5 completion record

Status: **owner-accepted complete on 2026-07-28**.

The accepted hosted application is the restricted Preview at
`https://math-word-hunt-preview-desmondquayson-bright-path-ed-tech.vercel.app`.
This Preview does not replace the canonical v7 GitHub Pages game.

## Hosted verification

- The stable Preview alias was ready behind Vercel Standard Protection. An
  anonymous request was challenged, while the existing approved automation
  bypass reached the application without exposing its value.
- Two synthetic teacher accounts each signed in and reached the authenticated
  teacher route. Refresh persistence, full browser-session restoration,
  logout, and denial of signed-out access were verified.
- Reciprocal cross-account tests denied reads and forged updates to the other
  teacher's protected class records. Each teacher's own records remained
  unchanged.
- Browser evidence recorded zero console errors, zero page errors, and zero
  non-aborted network failures. Expected navigation-aborted Next.js requests
  were not application failures.
- The protected Preview passed the accepted desktop and mobile viewport matrix,
  200% reflow, keyboard and visible-focus checks, reduced-motion and forced-
  colors checks, and canonical keyboard and Pointer Events gameplay checks.
  This evidence is not a formal WCAG conformance claim.

## Cleanup and security state

- Final hosted counts were: Auth users `0`, teacher profiles `0`, and teacher
  classes `0`. Associated synthetic activity, entitlement, deletion-request,
  and billing fixtures were also absent.
- Standard Protection remains enabled. Exactly one owner-controlled automation
  bypass remains, and its value was not placed in source, documentation, logs,
  or test artifacts.
- No successful Production deployment exists. No public exception was created
  and no secret was exposed. The final hosted authentication verification run
  made no further application-source, provider-configuration,
  environment-variable, canonical-game, historical-build, or backup-file
  changes.

Earlier owner-approved Phase 5 work included the canonical toast hit-testing
correction, hosted dependency-boundary correction, responsive overflow
correction, Vercel Preview configuration, Preview environment-variable
configuration, and Supabase Auth URL configuration. Those approved changes are
represented by the verified repository state and protected hashes recorded
below.

## Repository evidence

- Branch: `hosted-preview`
- Verified commit: `699d8afb635cefcd30eca1cb1d888269d3386a52`
- `docs/index.html` SHA-256:
  `10D0E49CD5DECF316615A10F6BDE37DC89796B2D8817EB1CF5D9EE25D263747E`
- `docs/vocab.js` SHA-256:
  `CAEB8FBB590FFFD8CBC169F88F174A38C26DE2D16A7E1B0C1CF5E83AC9F01C46`

## Deliberately deferred

Real email delivery, password-recovery delivery, live billing, pilot
invitations, a custom domain, and Production launch remain disabled and require
separate owner approval. Password recovery was not exercised because outbound
email delivery was prohibited for this phase.
