import { parseMapPrepDestination } from "@math-vocabulary-hunt/platform-core";
import { NextResponse } from "next/server";
import { inspectAdminAccess,validateAdminMutationCsrf } from "@/lib/admin/session";
import { createServiceSupabaseClient } from "@/lib/supabase/service";
function back(request:Request,result:string){const target=new URL("/admin",process.env.MVH_APPLICATION_ORIGIN??request.url);target.searchParams.set("section","map-prep");target.searchParams.set("map",result);return NextResponse.redirect(target,303)}
export async function POST(request:Request){const access=await inspectAdminAccess();if(access.state!=="authorized")return new NextResponse("Not Found",{status:404});const form=await request.formData();if(!await validateAdminMutationCsrf(form))return back(request,"csrf-denied");const destination=parseMapPrepDestination({label:String(form.get("label")??""),destinationUrl:String(form.get("destinationUrl")??""),adminDestinationUrl:String(form.get("adminDestinationUrl")??""),enabled:form.get("enabled")==="true",openMode:String(form.get("openMode")??"")},new Date().toISOString());if(!destination)return back(request,"invalid-destination");const client=createServiceSupabaseClient();if(!client)return back(request,"failed-closed");const content={key:"map-prep",title:destination.label,description:"Owner-managed external MAP Prep destination.",seoTitle:destination.label,seoDescription:"Open the configured MAP Prep application.",socialTitle:destination.label,socialDescription:"Owner-reviewed MAP Prep destination.",blocks:[{type:"external-link",href:destination.destinationUrl,...destination}]};const seo={title:destination.label,description:"Open the configured MAP Prep application.",social_title:destination.label,social_description:"Owner-reviewed MAP Prep destination."};const documentId=String(form.get("documentId")??"");const response=documentId?await client.rpc("revise_cms_document",{p_actor_admin_id:access.admin.id,p_document_id:documentId,p_expected_lock_version:Number(form.get("lockVersion")),p_content:content,p_seo_metadata:seo}):await client.rpc("create_cms_document",{p_actor_admin_id:access.admin.id,p_document_key:"map-prep",p_document_kind:"configuration",p_content:content,p_seo_metadata:seo});return back(request,response.error?"failed-closed":documentId?"revision-saved":"draft-saved")}
