import { NextResponse } from "next/server";import { inspectAdminAccess,validateAdminMutationCsrf } from "@/lib/admin/session";import { createServiceSupabaseClient } from "@/lib/supabase/service";
function v(f:FormData,n:string){return String(f.get(n)??"").trim()}function back(r:Request,s:string){const u=new URL("/admin",process.env.MVH_APPLICATION_ORIGIN??r.url);u.searchParams.set("section","media-library");u.searchParams.set("media",s);return NextResponse.redirect(u,303)}
export async function POST(r:Request){const a=await inspectAdminAccess();if(a.state!=="authorized")return new NextResponse("Not Found",{status:404});const f=await r.formData();if(!await validateAdminMutationCsrf(f))return back(r,"csrf-denied");const c=createServiceSupabaseClient();if(!c)return back(r,"failed-closed");const x=await c.rpc("archive_cms_media",{p_actor_admin_id:a.admin.id,p_media_asset_id:v(f,"mediaId"),p_expected_lock_version:Number(v(f,"lockVersion"))});return back(r,x.error?"archive-blocked":"archived")}
